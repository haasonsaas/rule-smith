{
  "name": "rule-smith",
  "version": "1.0.0",
  "description": "A lightweight, repo-native agent that writes Cursor rules automatically",
  "main": "src/generate-cursor-rule.ts",
  "type": "module",
  "scripts": {
    "build": "bun build ./src/generate-cursor-rule.ts --outdir ./dist",
    "start": "bun run ./src/generate-cursor-rule.ts",
    "test": "bun test",
    "lint": "bunx eslint . --ext .ts"
  },
  "dependencies": {
    "@octokit/rest": "^20.0.2",
    "dotenv": "^16.4.5",
    "openai": "^4.28.0",
    "yaml": "^2.3.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@typescript-eslint/eslint-plugin": "^7.1.1",
    "@typescript-eslint/parser": "^7.1.1",
    "eslint": "^8.57.0",
    "typescript": "^5.3.3"
  }
} 